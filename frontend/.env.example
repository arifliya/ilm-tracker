# For local `npm run dev`. Copy to .env.local if you need to point at a
# backend other than http://localhost:8787/api (wrangler dev's default
# port — see backend/wrangler.toml and DEPLOY_RUNBOOK.md).
VITE_API_URL=http://localhost:8787/api
